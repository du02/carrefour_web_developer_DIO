const map = new Map();

// adding
map.set('apple', 'fruit');

// get
map.get('apple');

// delete - true or underfined (in case it doesn't exist | em caso que não exista)
map.delete('apple');

/**
 *      MAP vs OBJ | coleções chaveadas 
 * 
 *  - Maps podem ter chaves de qualquer tipo;
 *  - Maps possuem a propriedade 'length';
 *  - Maps são mais fáceis de iterar;
 *  - Utilizando o valor das chaves é desconhecido;
 *  - Os valores tem mesmo tipo.
 * 
 */

